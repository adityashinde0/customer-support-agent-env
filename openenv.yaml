version: "1.0"
project:
  name: "customer-support-openenv"
  description: "A real-world Customer Support simulation where an AI agent resolves tickets via KB searches and clarification."
  tags:
    - customer-support
    - nlp
    - openenv
environment:
  entrypoint: "environment:CustomerSupportEnv"